// backend/utils/dataFormatter.js

// Function to format data before sending it to the client
const formatData = (data) => {
  // Add any additional formatting or transformations to the data here
  return data;
};

module.exports = {
  formatData,
};
